/**
 * @file App.tsx
 * @description 此文件的路由逻辑已迁移到：
 *   - src/router/index.tsx（路由配置）
 *   - src/components/layout/AppLayout.tsx（布局组件）
 *   保留此文件仅作为向后兼容的空壳，不再被 main.tsx 使用。
 */

export default function App() {
  return null;
}
